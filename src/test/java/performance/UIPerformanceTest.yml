
# Script generated by Blazemeter Chrome Extension

modules:
  nose:
    ignore-unknown-actions: true

execution:
  - executor: selenium
    scenario: HubNode_False-Selenium
    blazegrid: true
    iterations: 100
    capabilities:
      browserName: chrome

scenarios:
  HubNode_False-Selenium:
    browser: Chrome  # available browsers are: ["Firefox", "Chrome", "Ie", "Opera"]
    generate-flow-markers: true
    headless: true
    timeout: 60s
    think-time: 0s
    requests:
      - label: Test
        actions:
          - go(https://www.overstock.com/club-o-rewards-program)
          - assertTitle(Club O | Overstock.com | Earn Rewards for Shopping)